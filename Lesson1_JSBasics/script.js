/*const people = [
  {id: 1, name: "Polina", job: "Front-End"},
  {id: 2, name: "Zhamilia", job: "Front-End"},
  {id: 3, name: "Vyacheslav", job: "PM"},
  {id: 4, name: "Daria", job: "Front-End"},
]

const result = people.filter(item => item.job ==='Front-End');

console.log(result)

const res = people.filter(item => item.id !== 2)

console.log(res, 'id')*/

/*const fruits =['Apple' , "Birne", "Orange", "Ananas"]
const numbers = [63, 2, 9, 3, 6, 16, 27, 57, 42, 98
]

const sortedFruits = numbers.sort ((a, b) => {
  return b - a
})

const sortFruits = numbers.sort ((a, b) => {
  return (b - a)*2
})

console.log(sortedFruits)
console.log(sortFruits)

const numbers = [63, 2, 9, 3, 6, 16, 27, 57, 42, 98
]

const result = numbers.map(num => num ** 2).sort( ( a, b) => b - a)

console.log(result)*/

/*let x,y,z,oper

x = prompt ('Type first number')
y = prompt ('Type second number')
oper = prompt ('Type of operator')

if (oper == '+') {
  z = Number(x) + Number(y)
}
if (oper == '-') {
  z = Number(x) + Number(y)
}
if (oper == '/') {
  z = Number(x) + Number(y)
}
if (oper == '*') {
  z = Number(x) + Number(y)
}*/


alert(z)